import 'package:flutter/material.dart';
//import 'package:prism_capstone_project/screens/Dashboard_Screen.dart';

class PigProfiles extends StatelessWidget {
  const PigProfiles({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pig Profiles')),
      body: const Center(child: Text('Pig Profiles Screen')),
    );
  }
}
